{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/go-app/main.go",
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/pkg/env/.env",
            "args": [
                "products"
            ]
        }
    ]
}